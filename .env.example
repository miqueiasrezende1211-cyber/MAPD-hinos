# API server
PORT=8080

# Database (PostgreSQL + Drizzle)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/mapd_hinos

# Mockup sandbox (Vite)
MOCKUP_PORT=8081
BASE_PATH=/__mockup

# Expo app (Hinario) local dev
HINARIO_PORT=8082
EXPO_PUBLIC_DOMAIN=localhost
EXPO_PUBLIC_REPL_ID=local
